---
// src/components/ui/LogoContainer.astro
---
<a href="/" id="logo-container" aria-label="Home" class="group">
  <img
    src="https://www.google.com/s2/favicons?domain=avengerz-japan.com&sz=128"
    alt="AVENGERZ Logo"
    class="logo-img"
  />
</a>

<style>
  #logo-container {
    position: fixed;
    top: 32px;
    left: 32px;
    z-index: 50;

    /* === 修正: 幅と高さを厳格に制限 === */
    width: fit-content;
    height: fit-content;
    max-width: 50px;

    opacity: 0;
    transform: translateY(-10px);
    pointer-events: none;

    background: #ffffff;
    border: 1px solid rgba(0, 0, 0, 0.04);
    border-radius: 16px;
    padding: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.02);
    transition: transform 0.2s ease, box-shadow 0.2s ease, opacity 0.4s ease;
  }

  #logo-container.visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  #logo-container:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.05);
  }

  .logo-img {
    width: 32px;
    height: 32px;
    border-radius: 8px;
    display: block;
  }
</style>